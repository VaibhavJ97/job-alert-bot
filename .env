# Copy this file to `.env` (lowercase, no .example) and fill in your values
# `.env` is in .gitignore so it never reaches GitHub
# These same values go into GitHub Secrets for the cron job

# Phase 3 - needed for AI scoring
GROQ_API_KEY=your_groq_key_here

# Phase 4 - needed for email digest
RESEND_API_KEY=your_resend_key_here
DIGEST_EMAIL_TO=vaibhavjaiswal1234@gmail.com
DIGEST_EMAIL_FROM=alerts@your-domain.example
