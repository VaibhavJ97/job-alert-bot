# Job Alert Digest - 2026-06-21

**Summary:** 0 new | 0 scored | 154 title-filtered | 189 desc-filtered | 291 desc fetches (25 unavailable) | 43 seeded | 2 fetch failures

## inovex Stellenangebote

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Vulcan Energy

First run. Seeded 16 matching postings. 26 dropped by title filter, 4 dropped by description filter. New postings will be flagged as NEW from next run on.

## INERATEC

First run. Seeded 4 matching postings. 1 dropped by title filter, 2 dropped by description filter. New postings will be flagged as NEW from next run on.

## Aleph Alpha

FETCH FAILED. Will retry next run.

## KIT Career Service Jobboerse

First run. Seeded 0 matching postings. 0 dropped by title filter, 1 dropped by description filter. New postings will be flagged as NEW from next run on.

## Disy Informationssysteme

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## GBM Baugrundinstitut

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Deutsche Erdwaerme

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Solexperts

First run. Seeded 0 matching postings. 0 dropped by title filter, 2 dropped by description filter. New postings will be flagged as NEW from next run on.

## atmosfair

First run. Seeded 5 matching postings. 10 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Frequentis (DE)

First run. Seeded 9 matching postings. 10 dropped by title filter, 31 dropped by description filter. New postings will be flagged as NEW from next run on.

## Frequentis (EN)

First run. Seeded 8 matching postings. 7 dropped by title filter, 36 dropped by description filter. New postings will be flagged as NEW from next run on.

## Impossible Cloud

First run. Seeded 19 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## TYPO3

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Ostrom

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## NTT Data

First run. Seeded 9 matching postings. 7 dropped by title filter, 5 dropped by description filter. New postings will be flagged as NEW from next run on.

## ZeroEx

First run. Seeded 0 matching postings. 1 dropped by title filter, 1 dropped by description filter. New postings will be flagged as NEW from next run on.

## Reonic

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## init SE

FETCH FAILED. Will retry next run.

## Heidelberg Materials (Search)

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Heidelberg Materials (Workday)

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Arcadis (Early Careers)

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Arcadis (Professional)

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Bruker

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Fichtner

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## CONET

First run. Seeded 9 matching postings. 5 dropped by title filter, 6 dropped by description filter. New postings will be flagged as NEW from next run on.

## sovanta

First run. Seeded 0 matching postings. 6 dropped by title filter, 4 dropped by description filter. New postings will be flagged as NEW from next run on.

## Bureau Veritas

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Fugro

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## GoGeoGo

First run. Seeded 0 matching postings. 0 dropped by title filter, 61 dropped by description filter. New postings will be flagged as NEW from next run on.

## Turn2X

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## ITS Gruppe

First run. Seeded 7 matching postings. 23 dropped by title filter, 12 dropped by description filter. New postings will be flagged as NEW from next run on.

## K+S

First run. Seeded 0 matching postings. 2 dropped by title filter, 6 dropped by description filter. New postings will be flagged as NEW from next run on.

## GKW Consult

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Unique Land Use

First run. Seeded 1 matching postings. 1 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Unique Land Use (Personio)

First run. Seeded 1 matching postings. 1 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Atmen

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Ecoplanet

First run. Seeded 1 matching postings. 2 dropped by title filter, 1 dropped by description filter. New postings will be flagged as NEW from next run on.

## Proxima Fusion

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## DigiFarm

First run. Seeded 0 matching postings. 0 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## Feld Energy

First run. Seeded 3 matching postings. 16 dropped by title filter, 1 dropped by description filter. New postings will be flagged as NEW from next run on.

## Herrenknecht (DE)

First run. Seeded 0 matching postings. 0 dropped by title filter, 1 dropped by description filter. New postings will be flagged as NEW from next run on.

## Herrenknecht (EN)

First run. Seeded 0 matching postings. 0 dropped by title filter, 1 dropped by description filter. New postings will be flagged as NEW from next run on.

## DEKRA (IT-Sicherheit)

First run. Seeded 8 matching postings. 2 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.

## DEKRA (Germany)

First run. Seeded 2 matching postings. 4 dropped by title filter, 0 dropped by description filter. New postings will be flagged as NEW from next run on.
